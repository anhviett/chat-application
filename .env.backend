# MongoDB (used by DATABASE_URL or @nestjs/config)
MONGO_USERNAME=user
MONGO_PASSWORD=password
MONGO_DATABASE=db

# JWT signing secret (used by your Auth module)
JWT_SECRET=a_super_secret_jwt_key

# (Optional) Override default Nest port
PORT=3000
